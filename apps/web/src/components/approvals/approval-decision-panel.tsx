export function ApprovalDecisionPanel() {
  return (
    <section
      aria-label="Approval decision review"
      className="rounded-2xl border border-line bg-white p-5 text-sm leading-6 text-slate-600 shadow-lift"
    >
      Approval decisions are routed through a guarded endpoint contract. Advanced workflows are deferred until the core MVP data layer is connected.
    </section>
  );
}
