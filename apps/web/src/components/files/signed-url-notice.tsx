export function SignedUrlNotice() {
  return (
    <section
      aria-label="Signed URL notice"
      className="rounded-md border border-line bg-panel p-4 text-sm leading-6 text-slate-600"
    >
      Signed URL generation is a permission-checked placeholder with a short TTL. External object storage integration is deferred.
    </section>
  );
}
