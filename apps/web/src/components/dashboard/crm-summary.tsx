import { SummaryCard } from './summary-card';

export function CRMSummary() {
  return (
    <SummaryCard detail="Lead and opportunity placeholders from CRM basic." label="CRM" value="8 / 3" />
  );
}
