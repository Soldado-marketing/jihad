import { EmptyState } from '@/components/states/empty-state';

export default function SettingsPage() {
  return (
    <EmptyState
      title="Settings placeholder"
      description="Sprint 2 only reserves the settings route and shell placement. Advanced settings are implemented later."
    />
  );
}
